(()=>{const root=document.querySelector('#players'),input=document.querySelector('#playerSearch'),count=document.querySelector('#count');if(!root)return;fetch('../data/canonical/G-S01.json').then(r=>r.json()).then(s=>{const rows=s.members||[],e=v=>String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));function render(){const q=(input.value||'').trim().toLocaleLowerCase('fa'),list=rows.filter(p=>[p.display_name,p.snapshot_member_key,p.role,p.rank].join(' ').toLocaleLowerCase('fa').includes(q));count.textContent=list.length+' نتیجه';root.innerHTML=list.map(p=>`<a class="player-card" href="player.html?id=${encodeURIComponent(p.snapshot_member_key)}"><header><div><span class="rank">#${p.rank}</span><h3>${e(p.display_name)}</h3><small>${e(p.role||'Member')}</small></div></header><div class="player-stats"><div><span>استیج</span><strong>${p.stage??'—'}</strong></div><div><span>مدال لیگ</span><strong>${Number(p.league_medals||0).toLocaleString('en-US')}</strong></div><div><span>کیل</span><strong>${Number(p.total_kills||0).toLocaleString('en-US')}</strong></div></div></a>`).join('')||'<div class="empty">بازیکنی پیدا نشد.</div>';}input.addEventListener('input',render);render();}).catch(()=>root.innerHTML='<div class="empty">داده اعضا قابل بارگذاری نیست.</div>');})();